# Ввод чисел в формате a + b
def calc(a, sign, b):
    if sign == "+":
        return (a+b)
    if sign == "-":
        return (a-b)
    if sign == "*":
        return (a*b)
    if sign == "/":
        return (a/b)
